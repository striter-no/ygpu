{
  "metadata": {
    "name": "yellowstone-core",
    "version": "0.0.0"
  },

  "dependencies": {
    "example_device": { "version": ">=0.0.0" },
    "example_window": { "version": ">=0.0.0" },
    "example_compute": { "version": ">=0.0.0" }
  },

  "only-deps": true,
  "defaults": "defaults.json",
  "target": "valgrind",
  "testsPath": ".tests"
}
