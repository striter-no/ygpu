{
  "metadata": {
    "name": "project-tests",
    "version": "0.0.0"
  },

  "compiler": {
    "binary": "executable"
  },

  "code": {
    "sources": ["*.cpp"],
    "includes": ["include"],
    "output": "./bin/main"
  },

  "dependencies": {
    "errors": { "version": ">=0.0.0" }
  },

  "target": "debug",
  "defaults": "defaults.json"
}
